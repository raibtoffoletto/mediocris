export enum Routes {
  main = '/',
  charts = '/charts',
  signIn = '/signIn',
}

export enum ApiRoutes {
  auth = '/api/auth',
  banners = '/api/banner',
  refuels = '/api/refuel',
}

export const RowsPerPage = 6;

export enum URLParams {
  key = 'action',
  edit = 'edit',
  add = 'add',
}

export enum Input {
  pass = 'password',
  text = 'text',
}
